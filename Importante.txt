⚠️ LEIA COM ATENÇÃO - GUIA DE DESENVOLVIMENTO v3.0 ⚠️

Este projeto foi migrado para a arquitetura "Unified Server" (Node.js serve tudo).
NÃO COPIE MAIS ARQUIVOS PARA O XAMPP/HTDOCS MANUALMENTE!

----------------------------------------------------------------------
COMO ATUALIZAR O SISTEMA (DEPLOY)
----------------------------------------------------------------------

1. Faça suas alterações no código (Frontend ou Backend).
2. Salve os arquivos.
3. Vá na pasta raiz do projeto.
4. Execute o arquivo: DEPLOY_AUTOMATICO.bat

O que esse script faz sozinho?
[1] Compila o React (npm run build).
[2] Limpa a pasta pública do servidor (mantendo os uploads seguros).
[3] Copia o novo site para dentro do Backend.
[4] Reinicia o serviço no Windows (PM2).

----------------------------------------------------------------------
COMO FAZER BACKUP (SEGURANÇA) - FAÇA ISSO REGULARMENTE!
----------------------------------------------------------------------
Antes de qualquer alteração grande, garanta a segurança dos dados.

1. Execute o arquivo: BACKUP_TOTAL_V3.bat
2. Verifique se a pasta foi criada em E:\BlueFrog\Backups_Portal

Isso salva TUDO (Banco de Dados + Códigos + PDFs + Imagens).

----------------------------------------------------------------------
ONDE ESTÃO AS COISAS AGORA?
----------------------------------------------------------------------

- Código do Site:   /frontend/src
- Código da API:    /backend/index.js
- Imagens/PDFs:     /backend/public  (É aqui que os uploads ficam salvos!)
- Banco de Dados:   MySQL do XAMPP (Porta 3306)

----------------------------------------------------------------------
DÚVIDAS TÉCNICAS
----------------------------------------------------------------------
Se o site não abrir:
1. Abra o PowerShell como Admin.
2. Digite: pm2 list (Veja se está online).
3. Digite: pm2 logs (Para ver erros).

Ass: Marcelo Santos 🐸💙